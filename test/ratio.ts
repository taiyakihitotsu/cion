import type { Equal } from '../src/util.js'
import type {Scaling, DecimalRatio, ForceRatio, Trunc, ForceNat, Floor, RatioStr, SimplifyStr, GCM, LCD, Commonize, Add, Sub, Mul, Div, BitR, Relation, Abs, Not, IsZero, IsInt, IsNat} from '../src/ratio.js'
